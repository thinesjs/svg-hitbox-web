# Hitbox Labeller v2 — Select/Move/Resize + shadcn Migration

## Problem

The current hitbox labeller can only create hitboxes — once drawn, they cannot be moved or resized. Editing requires deleting and redrawing. The UI uses bare inline CSS styles throughout instead of a component library. This makes the tool frustrating for precise hitbox placement and the code harder to maintain.

## Solution

Add select/move/resize interactions (Photoshop-style) and migrate the UI to shadcn components with Tailwind CSS classes.

## Tool Modes

Two modes, switchable via toolbar toggle or keyboard shortcuts:

### Select Mode (V) — default
- **Click hitbox** → selects it, shows 8 resize handles, opens Edit Hitbox dialog
- **Drag hitbox body** → moves it (updates x, y in SVG coordinates)
- **Drag resize handle** → resizes it (updates x, y, width, height)
- **Click empty canvas** → deselects, closes dialog
- **Drag empty canvas** → pans the view
- **Click hitbox in sidebar** → selects it, opens dialog

### Draw Mode (D)
- **Click and drag** → draws new rectangle (dashed preview)
- **Release** → creates hitbox if > 5 SVG units each side, selects it, opens dialog, returns to Select mode
- **Drag empty canvas** → still pans (when not drawing)

## Resize Handles

8-handle model on selected hitbox:
- 4 corner handles (nw, ne, sw, se) — resize both dimensions
- 4 edge midpoint handles (n, s, e, w) — resize one dimension
- Handles are white squares with blue border, 10x10px screen size (scale-compensated so they stay the same screen size regardless of zoom)
- Cursor changes to appropriate resize cursor on hover (nwse, nesw, ns, ew)
- Minimum hitbox size enforced: 5 SVG units each dimension

## Move Behavior

- When cursor is over a selected hitbox body (not on a handle), cursor changes to `move`
- Dragging moves the hitbox, updating x/y coordinates
- If the Edit Dialog is open, coordinates update live during drag
- Unselected hitboxes cannot be moved — click first to select, then drag to move

## Edit Hitbox Dialog

Replaces the current bottom popover with a shadcn `Dialog`:
- Opens when a hitbox is selected (click on canvas or sidebar)
- Stays open while moving/resizing — coordinates display updates live
- Contains: hitbox ID, coordinates (x, y, width, height), built-in fields (mode, route, stop), custom fields with add/remove
- Uses shadcn `Input`, `Label`, `Button` components
- Dialog is non-modal (does not block canvas interaction) — use `modal={false}` on Dialog or position it as a floating panel

**Important:** The dialog must NOT block pointer events on the canvas. The user needs to interact with both the dialog fields AND the canvas simultaneously (e.g., select a hitbox, type metadata, then drag to adjust position without closing the dialog).

## shadcn Migration

### Init
```bash
pnpm dlx shadcn@latest init -t vite --preset avp4rpa
```

### Components to add
- `dialog` — Edit hitbox metadata
- `button` — Toolbar buttons, action buttons
- `input` — Field values, search
- `label` — Field labels
- `toggle-group` — Mode switcher (Select/Draw)
- `tooltip` — Toolbar button hints
- `scroll-area` — Sidebar hitbox list
- `separator` — Visual dividers
- `kbd` — Keyboard shortcut hints

### Style Migration
- Replace all `React.CSSProperties` inline style objects with Tailwind classes
- Replace CSS variable theme (`--bg`, `--surface`, etc.) with Tailwind/shadcn theme
- Remove `index.css` CSS variable definitions (shadcn provides its own theme)
- Keep bare CSS only for SVG overlay elements that need computed values (transform, dynamic positioning)

## Keyboard Shortcuts

| Key | Action |
|-----|--------|
| `V` | Switch to Select mode |
| `D` | Switch to Draw mode |
| `Delete` / `Backspace` | Delete selected hitbox |
| `Escape` | Deselect / exit draw mode |
| `Scroll` | Zoom canvas |

## Data Model

No changes to the Hitbox interface — x, y, width, height are updated in-place during move/resize operations.

```ts
interface Hitbox {
  id: string;
  x: number;       // updated during move
  y: number;       // updated during move
  width: number;   // updated during resize
  height: number;  // updated during resize
  fields: Record<string, string>;
}
```

## File Changes

### Modified files:
- `src/App.tsx` — mode state, shadcn dialog integration, updated event flow
- `src/SvgCanvas.tsx` — select/move/resize logic, handle rendering, mode-aware pointer handlers
- `src/HitboxSidebar.tsx` — migrate to shadcn components (Button, Input, ScrollArea, ToggleGroup, Separator, Kbd)
- `src/HitboxEditor.tsx` — rewrite as shadcn Dialog content (Input, Label, Button)
- `src/index.css` — remove custom CSS variables, use Tailwind/shadcn theme
- `src/types.ts` — add `ToolMode` type

### New files:
- `src/components/ui/*` — shadcn component files (generated by `shadcn add`)

## Interaction Details

### Pointer Event Priority (Select Mode)
1. If pointer is on a resize handle → resize
2. If pointer is on selected hitbox body → move
3. If pointer is on unselected hitbox → select it (on click, not drag)
4. If pointer is on empty canvas → pan (on drag) / deselect (on click)

### Coordinate Updates During Move/Resize
- Store drag start position and original hitbox bounds
- On each pointer move, compute delta in SVG coordinates
- Update hitbox state (triggers re-render of overlay + dialog)
- On pointer up, finalize the position
